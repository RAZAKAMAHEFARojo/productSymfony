{% extends 'base.html.twig' %}

{% block title %}Prime{% endblock %}

{% block body %}
    <h1>Prime</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prime.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ prime.type }}</td>
            </tr>
            <tr>
                <th>Paymentmode</th>
                <td>{{ prime.paymentmode }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ prime.amount }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ prime.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_prime_index') }}">back to list</a>

    <a href="{{ path('app_prime_edit', {'id': prime.id}) }}">edit</a>

    {{ include('prime/_delete_form.html.twig') }}
{% endblock %}
